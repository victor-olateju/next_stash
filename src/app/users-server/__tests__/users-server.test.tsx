import {render} from "@testing-library/react"
import UsersServer from "../page"
import { describe, it } from "node:test"


describe('UsersServer', () => {
    it('Should render "Users Server" heading', () => {
        render(<UsersServer />)

        // const users = 
    })
})